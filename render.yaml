services:
  - type: web
    name: aegis-main
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn aegis_main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
