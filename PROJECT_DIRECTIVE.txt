================================================================================
PERSONAL AI ASSISTANT — EVOLUTION, PRODUCTION-GRADE SaaS & CINEMATIC README
MASTER ENGINEERING DIRECTIVE — POST-FREEZE DEVELOPMENT AUTHORIZATION
================================================================================

PURPOSE
This directive supersedes the previous DEVELOPMENT FREEZE for Personal AI Assistant.
The project is UNFROZEN for the approved evolution below. Execute autonomously.
Scope is Personal AI Assistant ONLY. Do not mix any other project/domain into it.

REPOSITORY
https://github.com/aminazimi42-coder/Personal-ai-assistent
LIVE
https://personal-ai-assistent.onrender.com
BASE RELEASE
v1.0.0 / f02a32f2c8a5a997dfb6240297d6f2128bcd2e04 / Apache-2.0

================================================================================
1. ABSOLUTE AGENT CONTROL CONTRACT
================================================================================

1. Read this entire directive before modifying anything.
2. Verify the actual repository/GitHub baseline first; do not blindly trust this file.
3. Preserve all working functionality unless a change is explicitly required here.
4. Execute ALL approved work autonomously. Do not stop for phase-by-phase approval.
5. Do not ask the user to choose implementation alternatives when this directive
   already defines the target.
6. Do not perform unrelated refactors, features, experiments, redesigns, dependency
   additions, domain imports, or roadmap items.
7. No reset, rebase, squash, amend, force-push, history rewrite, or blind overwrite.
8. No fabricated tests, evidence, metrics, security claims, production claims, or
   completion claims.
9. Do not delete working capabilities merely to simplify implementation.
10. If unspecified, choose the smallest production-grade implementation consistent
    with the existing architecture and this directive.
11. Continue through all phases without stopping merely because work is large.
12. A genuine technical impossibility may block only that operation; record the exact
    blocker truthfully and continue every independent part that can be completed.
13. Secrets, tokens, passwords, private user data and credentials must never enter
    source, tests, logs, screenshots, README, commits or releases.
14. Render operations remain outside coding scope except application changes required
    by this directive; do not alter operational deployment settings gratuitously.
15. After completion DO NOT re-freeze the project. It remains actively maintained.

MANDATORY COHERENT-PHASE PROTOCOL:
IMPLEMENT → TEST → DEBUG → VERIFY → INSPECT DIFF → SECURITY CHECK → REGRESSION
CHECK → MEANINGFUL COMMIT → PUSH → VERIFY REMOTE

No empty/fake commits. Every meaningful phase must be remotely verified.

================================================================================
2. BASELINE — VERIFY, DO NOT BLINDLY REPEAT
================================================================================

The project previously completed its first production foundation and v1.0.0 release.
Known foundation includes:
- Flask app factory and centralized configuration
- PostgreSQL + connection pooling
- Alembic/Flask-Migrate
- authentication, token expiry and modern token hashing
- user isolation
- rate limiting and security headers
- AI quota + usage accounting
- request IDs, latency and AI token metadata
- tests and CI
- Gunicorn/Render deployment
- README/HANDOFF/CHANGELOG/LICENSE
- Apache-2.0
- v1.0.0 release

Do not repeat these blindly. Extend them where required by this directive.

KNOWN RESIDUAL SECURITY WORK:
Audit and harden/remove the legacy raw auth_token compatibility path so raw bearer
secrets are no longer stored or accepted unless an explicitly justified migration
requires temporary compatibility. Preserve expiry/revocation and test the migration.
If code changes require a release after v1.0.0, preserve v1.0.0 as immutable history
and use the correct semantic maintenance/minor version.

KNOWN DOCUMENTATION WORK:
Rebuild README to the cinematic standard in Section 7.

KNOWN VISUAL WORK:
Complete a verified Personal AI Assistant Screenshot Showcase.

================================================================================
3. PRODUCT TARGET
================================================================================

Evolve the existing Personal AI Assistant into a real, productive Production-Grade
SaaS platform without replacing its core architecture.

TARGET PATH:
Security → Persistence → Unified Retrieval → Memory → Agent Execution → Tool
Gateway → Verification → Automation → Cost/Token Intelligence → Privacy → Control
Center → SaaS/Billing → Multi-Tenant Architecture → API Productization →
Production-Like Integration Testing → Continuous AI Evaluation → Cinematic Product UX

A capability is COMPLETE only when it is actually implemented, persistent where
required, exposed through the proper authenticated product/API surface, isolated by
user/workspace/tenant, tested, security-reviewed, regression-tested, documented
truthfully, and end-to-end verified where applicable.

================================================================================
4. APPROVED EVOLUTION — ONLY THESE VALUE-ADDING ITEMS
================================================================================

4.1 CODE RETRIEVAL + UNIFIED KNOWLEDGE RETRIEVAL
-----------------------------------------------
Implement:
USER REQUEST → INTENT → RETRIEVAL PLAN → HYBRID SEARCH → RANK → CONTEXT
COMPRESSION → TOKEN/COST GUARD → LLM/AGENT → RESPONSE/ACTION → VERIFICATION

Required:
- repository ingestion
- strict user/repository/workspace isolation
- intelligent code chunking
- function/class/method/module symbol extraction
- semantic search
- keyword search
- symbol search
- dependency-aware search
- hybrid retrieval
- relevance ranking
- context compression
- incremental indexing
- safe caching
- cache invalidation after code changes
- retrieval latency/quality metrics
- token and estimated-cost measurement
- Minimum Sufficient Context: never sacrifice correctness for token reduction

Extend the same coherent retrieval architecture to code, documents, memories,
tasks, notes, projects and workspace knowledge.
Evaluate PostgreSQL + pgvector before adding another vector system. Do not add a
vector database merely because it is fashionable.

Required measurements:
input tokens without retrieval; input tokens with retrieval; tokens saved; context
size; retrieval latency; relevance/quality; estimated cost.

4.2 PERSONAL AI MEMORY ENGINE
-----------------------------
Implement durable layered memory:
- short-term conversation
- project memory
- task memory
- long-term preference memory
- relevance retrieval/ranking
- conflict handling
- retention
- deletion
- privacy controls
- user-visible management

UI must allow list/inspect/edit/delete and explicit “remember this” behavior.
Memory must be durable and user-isolated.

4.3 AGENTIC TASK EXECUTION ENGINE
---------------------------------
Real execution pipeline:
PLAN → RETRIEVE → ANALYZE → PROPOSE → APPROVAL → EXECUTE → VERIFY → EVIDENCE → REPORT

Required:
- persistent agent runs
- explicit state machine
- idempotency
- retries
- cancellation
- failure handling
- human approval for sensitive actions
- execution evidence
- audit trail
- user/workspace isolation

Never report an action as complete without verification evidence.

4.4 AI TOOL GATEWAY + PERMISSION SYSTEM
---------------------------------------
One centralized tool boundary with:
- read-only / write / destructive / external-communication classes
- least privilege
- allowlists
- tool identity + caller identity
- workspace/tenant boundary
- authorization
- approval for sensitive/destructive actions
- audit
- secret isolation
- timeout and bounded input/output
- safe failure

Prepare the architecture for MCP compatibility. MCP must never bypass this gateway.
Do not add arbitrary tools outside this approved boundary.

4.5 AI RELIABILITY + VERIFICATION ENGINE
----------------------------------------
Mandatory contract:
ACTION → EXECUTION → VERIFICATION → EVIDENCE → RESULT

A successful HTTP response is not automatically proof of business success.
Verification must be domain-specific, observable and persisted for important actions.

4.6 PERSONAL AUTOMATION ENGINE
------------------------------
Convert process-local/in-memory automation into durable product behavior:
TRIGGER → CONDITION → AI PROCESSING → ACTION → VERIFICATION

Required:
- persistent automations
- scheduler/worker
- retries
- idempotency
- failure states
- pause/resume
- audit
- permissions
- cost limits
- execution history

Approved examples only: scheduled summaries, overdue alerts, project analysis,
memory maintenance and reminder processing.

4.7 PROJECT WORKSPACE SYSTEM
----------------------------
Persist:
Workspace → Projects → Files → Documents → Tasks → Conversations → Memories →
AI Context → Agent Runs → Automations

Required durable DB state, authenticated API, strict ownership/isolation, lifecycle
operations, deletion and auditability. Never claim DB-backed while state is in-memory.

4.8 COST + TOKEN INTELLIGENCE
-----------------------------
Implement:
- input/output token tracking
- retrieval savings
- context budgets
- per-user and per-workspace limits
- daily/monthly controls
- cost estimation
- model routing
- cache awareness
- usage dashboard

Every AI/cost-producing route must share one consistent quota/accounting policy.
Audit every route, specifically ai-to-task and transcription, for quota bypass.

4.9 LLM PROVIDER ABSTRACTION
----------------------------
Create a provider-neutral boundary such as LLMProvider.
Application logic must not be coupled to one concrete provider.

Minimum architecture:
- OpenAI adapter
- OpenAI-compatible provider abstraction
- optional local-provider interface for future use

Required timeout, bounded retries, normalized errors, cost metadata,
provider/model telemetry and safe fallback policy where justified.
Do not activate providers merely for marketing.

4.10 PRIVACY-FIRST / LOCAL-AWARE ARCHITECTURE
---------------------------------------------
Implement operational privacy controls:
- data classification
- AI data boundary
- retention
- deletion
- account deletion
- user data export
- memory deletion
- secret isolation
- privacy-safe logging
- user/workspace/tenant isolation

Sensitive data must not leak into logs, telemetry, prompts or caches.
Add Privacy Policy product/documentation surface where required.

4.11 SERVER-SIDE REMINDERS + BACKGROUND JOBS
--------------------------------------------
Move scheduled work out of request threads/browser-only behavior:
Scheduler/Queue → Worker → Job → Verification → Audit

Use for reminders, automations, long-running transcription and scheduled summaries.
Evaluate Redis/queue infrastructure pragmatically; do not add unnecessary systems.

4.12 VOICE → TASK END-TO-END
----------------------------
VOICE → TRANSCRIPTION → TASK EXTRACTION → USER CONFIRMATION → TASK CREATION → VERIFY

Require bounded upload, MIME/content validation beyond browser-provided type,
temporary/privacy-safe processing, no sensitive transcript logging, quota/cost
accounting, confirmation for ambiguous/destructive actions and full tests.

4.13 PERSONAL AI CONTROL CENTER
-------------------------------
Real product surface for:
usage, cost, tokens, retrieval savings, memories, projects, tasks, automations,
agent runs, tools, permissions, security and audit/activity.
Not a static mock dashboard.

4.14 MULTI-TENANT / SaaS FOUNDATION
-----------------------------------
Evolve safely to:
- tenant/workspace identity
- memberships
- roles
- invitations
- ownership
- tenant-scoped resources
- organization/workspace quotas
- tenant-aware audit
- tenant-aware metering

Prove isolation with tests; tenant tables alone are not proof.

4.15 BILLING / SUBSCRIPTIONS
----------------------------
Add a production-ready billing boundary:
- subscription model
- plan model
- free/pro or equivalent abstraction
- usage limits
- billing state
- payment-provider abstraction
- webhook handling
- idempotent webhook processing
- subscription lifecycle
- entitlement enforcement

Never expose real billing credentials.

4.16 API VERSIONING + OPENAPI
-----------------------------
Create stable versioned API, e.g. /api/v1/.
Provide generated OpenAPI with actual request/response contracts, auth/security
schemes, error model, pagination conventions and health/readiness contracts.
Never claim Swagger/OpenAPI unless an actual generated surface exists.

4.17 INTEGRATION / PRODUCTION-LIKE TESTING
------------------------------------------
Keep existing unit tests and add integration tests for:
- real PostgreSQL
- migration upgrade/downgrade
- seeded data
- concurrent quota behavior
- pool behavior
- user isolation
- workspace/tenant isolation
- multi-worker behavior
- API integration
- voice flow
- provider abstraction
- background jobs
- billing webhook idempotency
- agent execution lifecycle

Use Testcontainers PostgreSQL or smallest equivalent production-like environment.
Normal CI must not require paid AI calls. Use deterministic mocks/fixtures.
Live AI evaluation, if used, must be isolated and cost-bounded.

4.18 CONTINUOUS AI QUALITY / EVALUATION
---------------------------------------
Scenario-based evaluation families:
- task extraction
- Persian/English understanding
- retrieval relevance
- context sufficiency
- prompt-injection resistance
- user A/B isolation
- token budget enforcement
- cost controls
- agent safety
- tool permissions
- verification correctness
- voice-to-task extraction

Every eval requires input, expected property, measurable assertion, result, version
and reproducible fixture. Integrate deterministic regression evaluation into CI.

4.19 MCP-COMPATIBLE TOOL INFRASTRUCTURE
---------------------------------------
After the internal Tool Gateway is stable, provide MCP-compatible schemas while
retaining permission, approval, tenant isolation, audit, secret isolation and
sandboxing boundaries. MCP cannot bypass the gateway.

4.20 CONTEXT COMPILER
---------------------
Compile inspectable context from memories, code/docs, tasks and project data.
Record source, relevance, provenance, context/token budget and compression decisions.
Provide an explanation of why context was selected.
Correctness first; Minimum Sufficient Context second.

4.21 UNIFIED KNOWLEDGE LAYER
----------------------------
Unify retrieval semantics across code, files, documents, memories, tasks, projects,
conversations and notes under the same security boundary. No cross-user/tenant retrieval.

================================================================================
5. IMMEDIATE SECURITY HARDENING
================================================================================

A. LEGACY RAW AUTH TOKEN
- find every raw-token storage/read/compare path
- audit all callers
- migrate safely to hashed verification
- remove raw-token persistence/acceptance where compatibility permits
- preserve expiry/revocation
- add security/regression/migration tests
- verify no unintended raw token remains in runtime/schema
- do not mutate historical v1.0.0

B. AI QUOTA
Audit every AI/cost-producing route. Ensure ai-to-task and transcription cannot bypass
quota/usage accounting.

C. UPLOAD SECURITY
Validate MIME/content/signature where appropriate; enforce size/duration; sanitize
filenames; never log audio/transcripts.

D. SECURITY HEADERS
Review CSP, HSTS where appropriate, Permissions-Policy, CORS and token/cookie safety.

E. DISTRIBUTED RATE LIMITING
Ensure correctness across multiple workers; use shared storage only where required.

F. PROMPT INJECTION
Threat-model retrieval/tools and add regression/evaluation cases. Model output must
never independently authorize a privileged tool action.

================================================================================
6. DATA ARCHITECTURE RULES
================================================================================

PostgreSQL is the durable source of truth. Add only models required by implemented
functionality. Candidate durable areas:
users, sessions/tokens, workspaces, projects, memberships, tasks, appointments,
reminders, memories, documents/files metadata, code repositories/indexes,
retrieval metadata, agent runs/steps, tools/tool audit, automations/runs,
AI usage events, subscriptions/billing events and audit events.

Every tenant/workspace operation MUST enforce scope.
Use transactions for state transitions and idempotency for repeatable effects.
All schema changes use migrations. No DDL-on-request.

================================================================================
7. CINEMATIC README — AILORA README DESIGN REFERENCE ONLY
================================================================================

REFERENCE:
https://github.com/azimilab2025-ai/ailora/blob/main/README.md

The AILORA README was inspected ONLY to understand its cinematic documentation and
visual design language. Do NOT import aerospace/orbital/NASA/Oya/scientific content,
claims, architecture or terminology.

Personal AI Assistant README must be dramatically more professional than the current
basic README while remaining truthful and project-specific.

MANDATORY STRUCTURE:
1. Hero / Product Identity
2. Truth / Release Status block
3. Cinematic hero visual
4. Project Visual Gallery
5. Verified technology/status badges
6. Control Center / System Snapshot
7. Table of Contents
8. Connected Systems visual story
9. Why Personal AI Assistant
10. Capability Matrix: Implemented & Verified / Active Development / Planned
11. Evidence / System Journey
12. Architecture diagram(s)
13. Technology Stack
14. Quick Start
15. API Reference + actual generated OpenAPI link
16. Testing & Quality
17. Security & Privacy
18. AI / Retrieval / Token Architecture
19. Agentic Execution
20. Automation
21. Control Center
22. Roadmap containing ONLY future work
23. Project Timeline / Release History
24. Documentation Index
25. Project Links Hub
26. Author
27. Footer

CINEMATIC SYSTEM STORY SHOULD ADAPT THE REFERENCE CONCEPT TO:
Capture → Retrieve → Understand → Plan → Act → Verify → Remember

CONNECTED SYSTEMS SHOULD VISUALLY COMMUNICATE:
User → Workspace → Knowledge → Memory → AI → Tools → Action → Verification

CONTROL CENTER SNAPSHOT MAY SHOW ONLY VERIFIED VALUES:
live app, API, tests, coverage, AI provider, retrieval, memory, agent engine,
automation, security, deployment and version.

CAPABILITY MATRIX MUST NEVER call in-memory prototypes, mocks, placeholders or
unexposed helpers “Complete”.

VISUAL RULES:
- premium
- cinematic
- technical
- strong hierarchy
- high-contrast capable
- centered hero sections where appropriate
- visual storytelling between dense technical sections
- evidence tables below visual summaries
- selective badges
- no clutter
- no fake telemetry
- no fake status
- no irrelevant imagery
- GitHub-compatible SVG/GIF/WebP/static visuals only; no JS in README

================================================================================
8. AUTHOR VISUAL — REQUIRED
================================================================================

Author section must follow the cinematic Author concept observed in AILORA.

Display:
AMIN AZIMI
AI ARCHITECT

Positioning:
End-to-End AI Systems Architecture
AI Product Architecture
Evidence-Based AI
Production AI Systems

REQUIRED ENGINEER VISUAL:
Use the SAME Bob engineering-agent portrait visual referenced by the AILORA README:
AILORA asset path: docs/assets/bob-agent-portrait.svg

Reuse/obtain the corresponding approved visual asset in the Personal AI Assistant
repository and place it in the Author section as the hard-hat/safety-helmet engineer
visual requested by the owner. Adapt only the surrounding text/context to Personal
AI Assistant. Do not import AILORA meaning/content.

Do not substitute an unrelated portrait.

================================================================================
9. SCREENSHOT SHOWCASE
================================================================================

Complete a real verified gallery using only Personal AI Assistant assets.
Preferred evidence:
- home/dashboard
- AI interaction
- tasks/appointments
- control center
- API/OpenAPI
- live Render deployment
- GitHub repository
- architecture/workflow evidence

Every screenshot must be project-specific, readable, secret-free, privacy-safe and
have meaningful alt text/caption. Do not fabricate screenshots or metrics.

================================================================================
10. UX PRIORITIES
================================================================================

Optimize real daily value:
fast capture; clear AI interaction; visible memory; project/workspace organization;
reliable execution; verified actions; reminders; automation; transparent token/cost
usage; privacy controls; control center.

Voice flow must be practical. Important AI actions must be understandable and controllable.
No decorative UI that does not support approved workflows.

================================================================================
11. SERVICE ARCHITECTURE
================================================================================

Target conceptual layers:
HTTP/API
→ Authentication/Authorization
→ Application Services
→ Domain Services
→ Retrieval/Memory/AI
→ Agent/Tools/Automation
→ Persistence
→ External Providers

Cross-cutting:
security, observability, cost, audit, privacy, verification.

Routes orchestrate; services implement business logic. Avoid provider calls scattered
across routes and avoid circular dependencies.

================================================================================
12. OBSERVABILITY
================================================================================

Extend existing request IDs/latency logging with justified telemetry:
request latency, AI latency, retrieval latency, tokens, estimated cost, agent run
duration, automation success/failure, tool execution, verification result,
background-job state and errors.

Never log secrets or raw sensitive user content.

================================================================================
13. PERFORMANCE / SCALABILITY
================================================================================

Do not prematurely build enterprise-scale infrastructure. Ensure bounded queries,
indexes, pagination, pooling, background execution, justified caching, bounded AI
context/uploads, timeouts, retries, idempotency and safe concurrency.
Additional workers must not corrupt or diverge durable state.

================================================================================
14. DEPENDENCY POLICY
================================================================================

Before adding a dependency:
prove necessity; inspect maintenance/security posture; check license compatibility;
pin compatible version; update docs; add tests where behavior changes.
Do not upgrade solely for novelty. Separate dev/test dependencies where appropriate.

================================================================================
15. CI / QUALITY GATES
================================================================================

CI must cover actual system behavior:
unit tests; integration tests; security tests; migration validation; import/startup
smoke; secret scan; lint; type checks where applicable; README truth checks; and
deterministic AI evaluations.

Never claim CI green without actual green CI.

================================================================================
16. RELEASE / VERSIONING
================================================================================

v1.0.0 remains immutable historical evidence.
Do not rewrite it.
Post-v1.0.0 changes require meaningful semantic versioning:
patch = security/bug maintenance; minor = backward-compatible features; major =
breaking contracts.
Synchronize application version, README, CHANGELOG, release notes and tag/release.

================================================================================
17. DOCUMENTATION TRUTH CONTRACT
================================================================================

Every README/documentation capability claim must map to real implementation, real
API where applicable, real persistence where required, real tests and real evidence.
Use explicit states: Implemented & Verified / Active Development / Planned.
Never claim “Complete” for mock-only, in-memory-only, placeholder or unexposed behavior.

================================================================================
18. FINAL ACCEPTANCE CHECKLIST
================================================================================

ARCHITECTURE:
- durable product state
- clean service boundaries
- user/workspace/tenant isolation
- no critical architecture contradictions

SECURITY:
- legacy raw token hardened/removed
- no AI quota bypass
- secure upload path
- security headers
- rate limiting
- tool permissions
- prompt-injection defenses
- secret scan clean

AI:
- provider abstraction
- retrieval
- memory
- context compiler
- token/cost controls
- AI evaluation
- voice-to-task

AGENT:
- persistent runs
- approval
- tool gateway
- execution
- verification
- evidence
- audit

AUTOMATION:
- durable scheduler/worker
- retries
- idempotency
- verification
- audit

SaaS:
- tenancy/workspaces
- roles/memberships
- subscriptions
- usage/entitlements
- billing webhook safety

API:
- versioned API
- generated OpenAPI
- documented contracts

DATA:
- valid migrations
- integration tests
- concurrency tests
- isolation tests
- no data-loss regression

UX:
- productive core flows
- memory controls
- control center
- voice-to-task
- verified screenshots

README:
- cinematic
- project-specific
- visual
- evidence-based
- no stale claims
- AILORA-inspired design language only
- Bob hard-hat engineer portrait in Author section
- no unrelated AILORA domain content

CI/RELEASE:
- full regression passes
- security suite passes
- integration suite passes
- AI evaluation passes
- migration checks pass
- remote branch verified
- meaningful commits pushed
- version/CHANGELOG synchronized

================================================================================
19. FINAL INDEPENDENT AUDIT
================================================================================

Review the final system as an independent senior engineering team.
Inspect repository tree, migrations, routes, services, security, AI, retrieval,
memory, agents, tools, automation, billing, tenancy, tests, CI, README, assets,
version, CHANGELOG, license, git status and remote HEAD.

Search explicitly for:
TODO, FIXME, placeholder, mock, simulated, fake, in-memory, debug, print,
secret, password, token leakage, stale MVP/prototype language, unsupported
production claims, undocumented routes, quota bypasses, unscoped queries,
raw-token paths.

Resolve every finding that falls inside this directive before completion.

================================================================================
20. GIT CONTROL
================================================================================

Meaningful phase examples:
feat(retrieval): production hybrid retrieval
feat(memory): persistent memory controls
feat(agent): durable execution and verification
feat(tools): permissioned tool gateway
feat(automation): durable scheduler and runs
feat(saas): tenancy/subscription foundations
feat(ai): provider abstraction and evaluation
feat(api): versioned OpenAPI surface
security(auth): remove legacy raw token compatibility
docs(readme): cinematic production README
docs(showcase): verified product evidence gallery

Every coherent phase:
TEST → DEBUG → VERIFY → DIFF → SECURITY → REGRESSION → COMMIT → PUSH → REMOTE VERIFY

================================================================================
21. FINAL STATE
================================================================================

Do NOT re-freeze after completion.
The previous v1.0.0 freeze is historical; this directive authorizes active evolution.

When every applicable acceptance criterion above is genuinely satisfied, record:

PERSONAL AI ASSISTANT
PRODUCTION-GRADE SaaS
ENGINEERING-COMPLETE FOR THIS DIRECTIVE
SECURITY-VERIFIED
DATA-INTEGRITY-VERIFIED
AI-EVALUATED
DOCUMENTATION-COMPLETE
CINEMATIC README COMPLETE
RELEASE/VERSION SYNCHRONIZED
REMOTE-VERIFIED
ACTIVE MAINTENANCE — NOT FROZEN

No unrelated roadmap may be added during execution.

================================================================================
22. SHORT HERMES AGENT COMMAND
================================================================================

READ `PERSONAL_AI_ASSISTANT_EVOLUTION_PRODUCTION_SAAS_DIRECTIVE.txt` COMPLETELY.

This file is the sole execution authority for the current Personal AI Assistant
evolution. Verify the repository baseline first, then execute ALL approved work
inside it autonomously without phase approvals or unrelated work.

Implement only: legacy raw-token security hardening; quota enforcement; durable
persistence; unified code/knowledge retrieval; memory; context compiler; durable
agent execution; permissioned tool gateway; verification/evidence; automation;
background jobs; cost/token intelligence; LLM provider abstraction; privacy
operations; voice-to-task; control center; multi-tenant SaaS; subscriptions/
billing; API v1 + generated OpenAPI; integration/production-like tests; continuous
AI evaluation; MCP-compatible tool architecture; server-side reminders; and the
approved UX evolution.

Rebuild README completely using the cinematic documentation/visual language observed
in AILORA, but ONLY with Personal AI Assistant content. Build hero, truth/status
block, cinematic visual, verified gallery, badges, control snapshot, connected-system
story, capability matrix, architecture, evidence journey, stack, quick start, API,
testing, security/privacy, AI/retrieval/token architecture, agent, automation,
control center, future roadmap, documentation hub, links and Author section.

For Author, reuse the same Bob engineering-agent portrait asset referenced by AILORA:
`docs/assets/bob-agent-portrait.svg`, adapted only to Personal AI Assistant context,
with the requested hard-hat engineer visual.

Complete Screenshot Showcase. Test, debug, inspect diff, security-check,
regression-check, commit, push and remote-verify after every coherent phase.
Perform the final independent audit. Do not claim completion without evidence.
Do not ask for approval between phases. Do not stop merely because the work is large.
Finish the entire directive and leave the project ACTIVE MAINTENANCE — NOT FROZEN.
================================================================================
END OF MASTER ENGINEERING DIRECTIVE
================================================================================
